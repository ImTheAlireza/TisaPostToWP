# چیزی که داخل ایمیج نباید برود — وگرنه build هم کند می‌شود هم «کدِ کهنه در تصویر».
.git
.github
.venv
venv
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
# داده‌ها و لاگ روی سرور/host می‌مانند (TISA_DATA_DIR در ایمیج به volume اشاره می‌کند)
data/
logs/
OPTION/
docs/
tests/
# tisa-product-importer.zip نه: «افزونهٔ ZIP چه فیلدهایی را می‌خواند» همین فایل را
# می‌خواند، و بی‌آن خطِ `importer :` در --check-config معنی‌اش را از دست می‌دهد.
